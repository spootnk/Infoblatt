{
  "name": "formfillingtool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
